arr = [40, 20, 30, 10, 50]

def selection_sort(arr):
    n = len(arr)
    for i in range(n-1):
        low = i
        for j in range(i+1, n):
            if arr[j] < arr[low]:
                low = j
        
        arr[i], arr[low] = arr[low], arr[i]

selection_sort(arr)
print(arr)

